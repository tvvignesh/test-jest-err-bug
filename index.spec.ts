import { testThrow } from './src/throw';

describe('TestErr', () => {
    describe('#testThrow', () => {
        it('should throw an error generated by testThrow', async () => {
            const generateError = jest.fn().mockReturnValue(new Error('Mocked Error'));
            await expect(testThrow()).rejects.toThrow(/Mocked Error/);
        });
    });
});

export {};